# Easier navigation: .., ..., ~ and -
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~"
alias -- -="cd -"

# Shortcuts
alias d="cd ~/Desktop"
alias db="cd ~/Dropbox"
alias dl="cd ~/Downloads"
alias doc="cd ~/Documents"
alias df="cd ~/dotfiles"
alias ref="cd ~/Documents/Reference"
alias cl="clear"
alias p="python"

# Sourcing
alias so="source"
# Source the .profile
alias sop="source ~/.profile"

# programs
alias mvim='open -a "MacVim"'

# Start psql
alias start_psql='pg_ctl -D /usr/local/var/postgres -l logfile start'
alias stop_psql='pg_ctl -D /usr/local/var/postgres -l logfile stop'

# Postgres shortcuts for NI databases
# Adhoc database connections (ssanpietro user has permission to create schemas)
alias sanpadhoc='psql -h pentahodb3.colo.networkedinsights.com -d adhoc -U ssanpietro'
alias adhoc='psql -h pentahodb3.colo.networkedinsights.com -d adhoc -U adhoc'
# Socialsense database connection
alias socialsense='psql -h postgres2.colo.networkedinsights.com -d socialsense -U nireader'
# dev3 database connection
alias dev3='psql -h dev3.colo.networkedinsights.com -d adhoc -U niuser'
# dev1 database connection
alias dev1='psql -h dev1.colo.networkedinsights.com -d adhoc -U niuser'

# Start and stop the pentaho server, etc
function call_pentaho {
    # action should be either start, stop, restart, status
    action=$1
    # Just in case -- sometimes starting the ba server alone isn't enough, you
    # must also start postgresql
    if [ "$action" == "start" ] || [ "$action" == "restart" ] ; then
        /Applications/pentaho/ctlscript.sh start postgresql
    elif [ "$action" == "status" ] ; then
        /Applications/pentaho/ctlscript.sh status postgresql
    fi
    /Applications/pentaho/ctlscript.sh $action baserver
}
alias puc='call_pentaho'

# Always use color when ls-ing
alias ls='ls -GFh'
# List all files in long format
alias l="ls -l"
# Long format and hidden files
alias la="ls -al"
# List only directories
alias lsd='ls -l | grep "^d"'
# List all files, folders, and subfolders and files
alias lr='ls -R'
# Grep -- example usage: 'lg txt' short for 'ls grep | txt'
alias lg='ls | grep'
# Grep long version 
alias llg='ls -al | grep'
# Grep history
alias hg='history | grep'
# virtualenvwrapper -- show all virtual environments
alias lv='ls $WORKON_HOME'
# virtualenvwrapper - long version
alias llv='ls -al $WORKON_HOME'

# `cat` with beautiful colors. requires Pygments installed.
# if not already installed: sudo easy_install Pygments
# Use ccat rather than remapping cat because pygmentize only works on single
# files. Still want ability to merge multiple files using 'cat' which you can't
# do with pygmentize
alias ccat='pygmentize -O style=monokai -f console256 -g'

# git root
alias gr='[ ! -z `git rev-parse --show-cdup` ] && cd `git rev-parse --show-cdup || pwd`'

# Get file size
alias fs="stat -f \"%z bytes\""

# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"

# If terminal takes a long time to load, delete the *.asl log files and it will
# be much snappier again: http://forums.macrumors.com/showthread.php?t=987949
alias cllogs="sudo rm -f /private/var/log/asl/*.asl"

# List open ports
alias lsports='lsof -i -P | grep -i "listen"'
# Kill port number <PID>
alias killport='kill -9'

###
# Centro database access aliases
###

# ################
# Burnham DB Access Aliases #
# ################
alias bdms='psql -h 10.0.0.98 -d dm_staging -U dm_staging'

# ################
# Legacy DB Access Aliases #
# ################
# Production
alias edwtransis='psql -h etl.transis.net -d dw_transis -U dw'
alias epd='psql -h etl.transis.net -d dw_production -U dw_production'
alias eph='psql -h etl.transis.net -d hibernate_production -U hibernate_production'
alias epq='psql -h etl.transis.net -d quartz_production -U quartz_production'

# Staging
alias smmsstage='psql -h staging-db -d mms_staging -U dw'
alias esd='psql -h etl-db.transis.net -d dw_staging -U dw_staging'
alias esh='psql -h etl-db.transis.net -d hibernate_staging -U hibernate_staging'
alias esq='psql -h etl-db.transis.net -d quartz_staging -U quartz_staging'

# Test
alias etd='psql -h etl-db.transis.net -d dw_test -U dw_test'
alias eth='psql -h etl-db.transis.net -d hibernate_test -U hibernate_test'
alias etq='psql -h etl-db.transis.net -d quartz_test -U quartz_test'

# Development
alias ldwmms='psql -h localhost -d dw_transis -U dw'
alias ldd='psql -h localhost -d dw_development -U dw_development'
alias ldh='psql -h localhost -d hibernate_development -U hibernate_development'
alias ldq='psql -h localhost -d quartz_development -U quartz_development'

# Centro SSH aliases
alias setl='ssh pentaho@pdi01.staging.bi.transis.net'
alias sbi='ssh pentaho@web01.staging.bi.transis.net'
alias petl='ssh pentaho@pdi01.bi.transis.net'
alias pbi='ssh pentaho@web01.bi.transis.net'
alias warehouse='ssh pentaho@warehouse.transis.net'

# Start/stop the psql server
alias psql_start='pg_ctl -D /usr/local/var/postgres -l logfile start'
alias psql_stop='pg_ctl -D /usr/local/var/postgres -l logfile stop'

# Adhoc NI DB
alias adhoc='psql -h pentahodb3.colo.networkedinsights.com -d adhoc -U nireader'

##
# Tmux
##

# Tab completion for tmux sessions.
# Quickly open new tmux sessions in your projects dir.
# https://gist.github.com/ryandotsmith/1396797
#  
# Setup:
# Source this code in your bash shell.
# Update the code_dir var with the root directory of your source code.
#  
# Usage:
#  
# Use the tab to open an existing session.
# $ ta [TAB]
# 
# Arguments that are passed to ta will be used to create a new session.
# ta will open a new tmux session and set the default path to the found dir. 
# $ ta payments
# $ pwd
# /code_dir/payments 
ta()
{
  local session_name="$1"
  tmux attach-session -t "$session_name"
  if [ $? -ne 0 ]; then
    local code_dir="~"
    local list_of_dirs=( $(find "$code_dir" -name "$session_name" -type d ) )
    local first_found="${dirs[0]}"
    cd "$first_found"
    echo "ta() is creating new tmux session with name=$session_name"
    tmux new-session -d -s "$session_name"
    echo "ta() is setting default path with dir=$first_found"
    tmux set default-path "$first_found"
    tmux attach-session -t "$session_name"
  fi
}
_ta()
{
  COMPREPLY=()
  local session="${COMP_WORDS[COMP_CWORD]}"
  COMPREPLY=( $(compgen -W "$(tmux list-sessions 2>/dev/null | awk -F: '{ print $1 }')" -- "$session") )
}
complete -F _ta ta


# List tmux sessions
alias tls='tmux ls'
# Start a new tmux session
alias tns='tmux new -s'
# Kill tmux session
alias tk='tmux kill-session -t'
