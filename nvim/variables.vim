" Personal info
let g:my_name = 'Steve SanPietro'
let g:my_email = 'sanpietro.steve@gmail.com'

" Folder locations
let g:mru_file = $HOME . '/.vim_mru_files'
let g:xptemplate_personal_folder = [g:nvim_config_root . '/xpt-personal']

" Set the global default shell to not be the bash shell
let g:is_bash=0

" Set leader and localleader
let mapleader = ","
let maplocalleader = "\\"
